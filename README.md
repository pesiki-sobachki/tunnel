# 🚀 Руководство по созданию безопасных туннелей (Self-Hosted аналог ngrok)

Это руководство описывает, как расширить функциональность **Универсального HTTPS шлюза** для создания безопасных, стабильных и удобных туннелей от публичного сервера к локальной машине разработчика. Данное решение является простой, бесплатной и полностью контролируемой вами альтернативой популярным сервисам вроде `ngrok` или `localtunnel`.

## 📖 Содержание

1.  [**Философия и выбор инструмента**](#-философия-и-выбор-инструмента)
    *   [Зачем это нужно?](#зачем-это-нужно)
    *   [Сравнение альтернатив](#сравнение-альтернатив)
    *   [Почему SSH Reverse Tunnel — идеальный выбор](#почему-ssh-reverse-tunnel--идеальный-выбор)
2.  [**Концепция работы**](#-концепция-работы)
    *   [Как проходит запрос: от браузера до локального кода](#как-проходит-запрос-от-браузера-до-локального-кода)
3.  [**Пошаговая настройка: от теории к практике**](#-пошаговая-настройка-от-теории-к-практике)
    *   [Этап 1: Настройка на сервере (выполняется один раз)](#этап-1-настройка-на-сервере-выполняется-один-раз)
    *   [Этап 2: Создание туннеля с локальной машины (по необходимости)](#этап-2-создание-туннеля-с-локальной-машины-по-необходимости)
4.  [**Профессиональное использование: Надежность и безопасность**](#-профессиональное-использование-надежность-и-безопасность)
    *   [Использование SSH-ключей для входа без пароля](#использование-ssh-ключей-для-входа-без-пароля)
    *   [Автоматическое переподключение с `autossh`](#автоматическое-переподключение-с-autossh)
    *   [Безопасность: Создание выделенного пользователя для туннелей](#безопасность-создание-выделенного-пользователя-для-туннелей)
5.  [**Возможности и ограничения: Что можно туннелировать?**](#-возможности-и-ограничения-что-можно-туннелировать)
    *   [Основной принцип: Туннелирование на уровне TCP](#основной-принцип-туннелирование-на-уровне-tcp)
    *   [Сценарий 1: Веб-разработка (HTTP, WebSocket, gRPC)](#сценарий-1-веб-разработка-http-websocket-grpc)
    *   [Сценарий 2: Прямой проброс TCP (Базы данных, игры, SSH)](#сценарий-2-прямой-проброс-tcp-базы-данных-игры-ssh)
    *   [Что не поддерживается (UDP)](#что-не-поддерживается-udp)
    *   [Сводная таблица протоколов](#сводная-таблица-протоколов)
6.  [**Практические сценарии использования**](#-практические-сценарии-использования)
7.  [**Перспективы развития**](#-перспективы-развития)
8.  [**Часто задаваемые вопросы (FAQ) и решение проблем**](#-часто-задаваемые-вопросы-faq-и-решение-проблем)

---

## 1. 🤔 Философия и выбор инструмента

### Зачем это нужно?

В современной веб-разработке часто возникает необходимость предоставить доступ к локально запущенному приложению из внешней сети. Типичные задачи:
-   **Демонстрация проекта** заказчику или коллегам без развертывания на staging-сервере.
-   **Тестирование веб-хуков** от внешних сервисов (Stripe, GitHub, Telegram Bots), которые должны отправлять запросы на публично доступный URL.
-   **Отладка мобильных приложений**, которым нужен доступ к локально запущенному бэкенду.

Сервисы типа `ngrok` решают эту задачу, но имеют ограничения в бесплатной версии (случайные имена, лимиты по времени) и требуют доверия к сторонней компании, через которую пойдет ваш трафик. Создание собственного решения на базе существующей инфраструктуры дает полный контроль, безопасность и гибкость.

### Сравнение альтернатив

| Инструмент | Основная идея | Интеграция с вашим шлюзом | Вывод |
| :--- | :--- | :--- | :--- |
| `frp`, `chisel` | Мощные self-hosted решения с отдельным серверным компонентом. | **Средняя.** Требуют запуска и поддержки дополнительного сервиса, на который шлюз будет проксировать трафик. | Избыточны и усложняют инфраструктуру. |
| `localtunnel`, `sish` | Специализированные серверы, имитирующие `ngrok` с авто-субдоменами (`sish`) или случайными именами (`localtunnel`). | **Сложная/Конфликтующая.** Эти инструменты сами хотят быть reverse-proxy, что дублирует и конфликтует с функционалом вашего шлюза. | Не подходят для интеграции, так как нарушают существующую архитектуру. |
| **`ssh -R`** | Стандартная, встроенная в OpenSSH функция обратного туннелирования. **Никакого дополнительного ПО на сервере не требуется.** | **Идеальная.** Напрямую использует существующую возможность шлюза "проксировать на порт хост-машины". | **Лучший выбор.** Максимальная простота, безопасность и элегантность. Не добавляет новых сущностей и точек отказа. |

### Почему SSH Reverse Tunnel — идеальный выбор

Мы выбираем `ssh -R` (Reverse Tunnel), потому что это не *продукт*, а *функция* уже существующего, максимально надежного и безопасного компонента вашей системы — SSH-сервера. Этот подход полностью соответствует духу вашего HTTPS-шлюза: простота, надежность и использование стандартных, проверенных временем инструментов.

---

## 2. 🧠 Концепция работы

Чтобы понять, как все работает, проследим путь одного HTTPS-запроса:

1.  **Пользователь** открывает в браузере `https://dev.example.com`.
2.  **DNS** направляет запрос на IP-адрес вашего **публичного сервера**.
3.  **HTTPS шлюз (Nginx)** принимает запрос на порту 443.
    -   Терминирует TLS (расшифровывает HTTPS).
    -   Видит, что запрос предназначен для `dev.example.com`.
    -   Согласно своей конфигурации, проксирует (перенаправляет) теперь уже обычный HTTP-запрос на `localhost:8080` *внутри сервера*.
4.  **SSH-сервер** на сервере "ловит" этот запрос на порту `8080`, потому что с локальной машины был установлен обратный туннель.
5.  **SSH-туннель** безопасно пересылает этот запрос по зашифрованному SSH-соединению на **ваш локальный компьютер**.
6.  **SSH-клиент** на вашей машине получает запрос и перенаправляет его на `localhost:3000`.
7.  **Ваше локальное веб-приложение**, запущенное на порту `3000`, получает чистый HTTP-запрос и обрабатывает его.

Ответ от приложения проходит тот же путь в обратном порядке. Для конечного пользователя все выглядит так, будто он напрямую обращается к вашему приложению по защищенному HTTPS-соединению.

---

## 3. ⚙️ Пошаговая настройка: от теории к практике

### Этап 1: Настройка на сервере (выполняется один раз)

Этот этап "резервирует" субдомен и порт на сервере для будущего использования.

1.  **Настройте DNS:**
    В панели управления вашего домена создайте A-запись, указывающую на IP вашего публичного сервера.
    -   **Тип:** `A`
    -   **Хост:** `dev` (или любое другое имя: `tunnel`, `demo` и т.д.)
    -   **Значение:** `YOUR_SERVER_IP`

2.  **Добавьте домен в HTTPS шлюз:**
    Подключитесь к вашему серверу по SSH, перейдите в директорию со шлюзом и запустите интерактивный мастер добавления:

    ```sh
    cd /path/to/your/gateway
    ./gateway.sh add
    ```

    Ответьте на вопросы скрипта, выбрав опцию **проксирования на порт хост-машины**:

    -   `Введите домен`: `dev.example.com`
    -   `Введите ваш email`: `admin@example.com`
    -   `Куда проксировать трафик? [1-3]`: **`2`** (На порт хост-машины)
    -   `Введите порт на хост-машине`: `8080`
        > 💡 **Совет:** Вы можете выбрать любой свободный порт. `8080` — популярный выбор. Главное — запомнить его.
    -   `Использовать staging-сервер? (y/n)`: `n`

    **Готово!** Серверная часть настроена. Ваш шлюз теперь слушает `https://dev.example.com` и перенаправляет весь трафик на порт `8080` самого сервера.

### Этап 2: Создание туннеля с локальной машины (по необходимости)

Теперь, когда вам нужно открыть доступ к вашему локальному проекту, выполните одну команду в терминале на своем компьютере.

Предположим, ваше приложение запущено локально на порту `3000`.

```sh
ssh -R 8080:localhost:3000 user@dev.example.com
```

**Разберем команду:**

-   `ssh`: Ваш SSH-клиент.
-   `-R 8080:localhost:3000`: Флаг **R**everse tunnel.
    -   `8080`: **Удаленный порт** на сервере, который будет слушать SSH-сервер (тот же, что вы указали на Этапе 1).
    -   `localhost`: Адрес назначения **на вашей локальной машине**.
    -   `3000`: **Локальный порт**, на котором работает ваше приложение.
-   `user@dev.example.com`: Ваши учетные данные для SSH-доступа к серверу.

Пока это соединение активно, `https://dev.example.com` будет указывать на ваше локальное приложение. Чтобы остановить туннель, просто закройте SSH-сессию (нажмите `Ctrl+C`).

---

## 4. 🏆 Профессиональное использование: Надежность и безопасность

### Использование SSH-ключей для входа без пароля

Чтобы каждый раз не вводить пароль, настройте аутентификацию по ключу.

1.  **На локальной машине** (если у вас еще нет ключа):
    ```sh
    ssh-keygen -t rsa -b 4096
    ```
2.  **Скопируйте ключ на сервер:**
    ```sh
    ssh-copy-id user@dev.example.com
    ```
Теперь команда `ssh -R ...` будет выполняться мгновенно, без запроса пароля.

### Автоматическое переподключение с `autossh`

Обычное SSH-соединение может прерваться из-за смены сети или "засыпания" ноутбука. `autossh` — это утилита, которая автоматически восстанавливает туннель.

1.  **Установка на локальной машине:**
    ```sh
    # macOS (Homebrew)
    brew install autossh
    # Ubuntu/Debian
    sudo apt-get install autossh
    ```
2.  **Использование:**
    Команда почти идентична, просто замените `ssh` на `autossh` и добавьте флаг `-M 0`:
    ```sh
    autossh -M 0 -R 8080:localhost:3000 user@dev.example.com
    ```
    Теперь ваш туннель будет "самовосстанавливающимся" и невероятно стабильным.

### Безопасность: Создание выделенного пользователя для туннелей

Для максимальной безопасности рекомендуется создать на сервере специального пользователя, который может *только* создавать туннели, но не имеет доступа к оболочке (shell).

1.  **На сервере** создайте пользователя `tunneluser`:
    ```sh
    sudo useradd -m -s /usr/sbin/nologin tunneluser
    ```
2.  Создайте для него директорию `.ssh` и файл `authorized_keys`:
    ```sh
    sudo mkdir -p /home/tunneluser/.ssh
    sudo touch /home/tunneluser/.ssh/authorized_keys
    ```
3.  Скопируйте ваш **публичный SSH-ключ** (`id_rsa.pub` с локальной машины) в файл `/home/tunneluser/.ssh/authorized_keys` на сервере.
4.  **Важный шаг:** Отредактируйте этот файл (`sudo nano ...`) и добавьте в самое начало строки с ключом специальные опции, которые сильно ограничивают права:
    ```
    restrict,permitopen="localhost:8080" ssh-rsa AAAA...
    ```
    *   `restrict`: Запрещает все возможности (выполнение команд, X11, и т.д.).
    *   `permitopen="localhost:8080"`: Явно разрешает этому ключу пробрасывать трафик только на `localhost` сервера и только на порт `8080`. Это очень безопасная конфигурация.

5.  Установите корректные права:
    ```sh
    sudo chown -R tunneluser:tunneluser /home/tunneluser
    sudo chmod 700 /home/tunneluser/.ssh
    sudo chmod 600 /home/tunneluser/.ssh/authorized_keys
    ```
6.  Теперь используйте этого пользователя для создания туннеля:
    ```sh
    autossh -M 0 -R 8080:localhost:3000 tunneluser@dev.example.com
    ```

---

## 5. 🔬 Возможности и ограничения: Что можно туннелировать?

### Основной принцип: Туннелирование на уровне TCP

`ssh -R` создает универсальный туннель для протокола **TCP**. Это означает, что он может пересылать любые данные, которые передаются по TCP, не вникая в их содержимое.

### Сценарий 1: Веб-разработка (HTTP, WebSocket, gRPC)

Это основной сценарий, для которого используется связка с HTTPS-шлюзом.

-   **HTTP/HTTPS сайты и API:** Работают идеально. Nginx превращает HTTPS в HTTP, который без проблем летит через TCP-туннель.
-   **WebSocket (WSS):** Работают отлично. Современные конфигурации Nginx (включая, вероятно, вашу) по умолчанию поддерживают апгрейд соединения до WebSocket. SSH-туннель просто передает это установленное TCP-соединение.
-   **gRPC:** Работает, но требует, чтобы ваш Nginx был сконфигурирован для проксирования HTTP/2, так как gRPC работает поверх него.

### Сценарий 2: Прямой проброс TCP (Базы данных, игры, SSH)

Вы можете использовать `ssh -R` и **без HTTPS-шлюза**, чтобы сделать любой локальный TCP-сервис временно доступным из интернета.

> **Внимание!** В этом сценарии трафик не будет шифроваться с помощью TLS. Шифрование будет только внутри SSH-туннеля.

**Пример:** Сделать локальную базу данных PostgreSQL (порт `5432`) доступной на порту `15432` публичного сервера.

1.  **На сервере:** Откройте порт `15432` в файрволе.
2.  **На локальной машине:**
    ```sh
    ssh -R 15432:localhost:5432 user@dev.example.com
    ```
3.  Теперь вы можете подключиться к вашей локальной БД с другой машины через `dev.example.com:15432`.

### Что не поддерживается (UDP)

Стандартный OpenSSH **не умеет** туннелировать UDP-трафик. Это значит, что сервисы, которые критически зависят от UDP, работать не будут:
-   Некоторые онлайн-игры (особенно шутеры).
-   VoIP-сервисы (голосовая связь).
-   Протокол QUIC (основа HTTP/3).

### Сводная таблица протоколов

| Протокол/Приложение | Поддерживается? | Требуется ли HTTPS-шлюз? |
| :--- | :--- | :--- |
| **Веб-сайты (HTTP/API)** | ✅ **Да** | ✅ **Да** |
| **WebSocket (WSS)** | ✅ **Да** | ✅ **Да** |
| **Базы данных (PostgreSQL, MySQL)** | ✅ **Да** | ❌ **Нет** (прямой проброс) |
| **TCP-игры / кастомные TCP-сервисы** | ✅ **Да** | ❌ **Нет** (прямой проброс) |
| **SSH, RDP, VNC** | ✅ **Да** | ❌ **Нет** (прямой проброс) |
| **UDP-игры, VoIP, DNS, QUIC** | ❌ **Нет** | - |

---

## 6. ✨ Практические сценарии использования

-   **Frontend-разработчик** показывает новую фичу на `https://demo.example.com`, запустив `webpack-dev-server` у себя на машине.
-   **Backend-разработчик** отлаживает веб-хуки от платежной системы Stripe, указав в настройках Stripe URL `https://hooks.example.com/stripe`.
-   **DevOps-инженер** временно предоставляет безопасный доступ к внутренней панели управления, пробросив ее через туннель.
-   **Мобильный разработчик** тестирует приложение на реальном устройстве, которое подключается к API по адресу `https://api-dev.example.com`, указывающему на локальный бэкенд.

---

## 7. 🔮 Перспективы развития

Хотя текущее решение уже очень мощное, его можно улучшить:

1.  **Клиентские скрипты/алиасы:** Можно создать простые скрипты на локальной машине для еще более быстрого запуска туннелей (например, `start-tunnel 3000`).
2.  **Поддержка UDP:** Если поддержка UDP станет критически необходимой, можно будет развернуть на сервере дополнительный сервис (например, `frps` или `WireGuard`), который будет обрабатывать UDP-трафик, и настроить шлюз на работу с ним. Однако это значительно усложнит инфраструктуру.
3.  **Мониторинг:** Можно написать скрипт на сервере, который будет проверять активные SSH-соединения и показывать, какие туннели сейчас работают.

---

## 8. ❓ Часто задаваемые вопросы (FAQ) и решение проблем

**В: Я запускаю команду `ssh -R`, но сайт `https://dev.example.com` не открывается (ошибка 502 Bad Gateway).**
**О:** Это означает, что Nginx не может связаться с портом `8080` на сервере.
    -   **Проблема:** Ваш SSH-туннель не активен или работает неправильно.
    -   **Решение:** Убедитесь, что SSH-сессия с командой `ssh -R` запущена и не выдала ошибок. Проверьте, что вы используете правильные порты в команде (удаленный `8080` и локальный `3000`).

**В: Браузер выдает ошибку "Connection timed out" или "Не удается получить доступ к сайту".**
**О:** Это, скорее всего, проблема с сетью или файрволом.
    -   **Проблема:** Файрвол на сервере блокирует порты 80/443, либо ваш локальный файрвол блокирует исходящее SSH-соединение.
    -   **Решение:** Проверьте настройки файрвола на сервере (`ufw status`, `iptables -L`). Убедитесь, что ваш HTTPS-шлюз запущен и работает.

**В: При подключении SSH запрашивает пароль, хотя я настроил ключи.**
**О:** Наиболее частая причина — неправильные права доступа к файлам на сервере.
    -   **Проблема:** Директория `.ssh` или файл `authorized_keys` имеют слишком открытые права.
    -   **Решение:** Выполните на сервере `chmod 700 ~/.ssh` и `chmod 600 ~/.ssh/authorized_keys`. Проверьте логи SSH-сервера (`sudo journalctl -u ssh`) для получения детальной информации.
