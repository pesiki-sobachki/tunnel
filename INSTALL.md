---

# 🚀 Руководство по Развертыванию Тестовой Среды

Этот документ описывает пошаговый процесс настройки и развертывания решения для создания SSH-туннеля с использованием двух виртуальных машин.

## 📋 Предварительные требования

Перед началом убедитесь, что у вас есть:

1.  **Два виртуальных сервера:**
    *   **Удаленный сервер (REMOTE-VM):** Будет выполнять роль публичного шлюза.
    *   **Сервер приложения (LOCAL-VM):** Будет выполнять роль машины разработчика, на которой запускается приложение.
    *   *Рекомендуемая ОС для обеих машин: Ubuntu Server 22.04 LTS.*
2.  **SSH-доступ** к обеим машинам с правами `sudo`.
3.  **IP-адреса** обеих машин. Запишите их, они понадобятся на всех этапах.
4.  **Клонированный репозиторий** с этим проектом на вашем основном рабочем компьютере.

## 🏛️ Архитектура Среды

*   **Ваш Рабочий ПК:** Центр разработки. Здесь вы пишете код (например, в VS Code), работаете с Git и храните ваш приватный SSH-ключ.
*   **Удаленный сервер (REMOTE-VM):** Публичный шлюз. Принимает входящие HTTP-запросы с помощью **Nginx** и перенаправляет их в туннель.
*   **Сервер приложения (LOCAL-VM):** Исполнительная среда. На ней запускается ваше веб-приложение и скрипт `start_tunnel.sh`, который создает SSH-туннель к `REMOTE-VM`.

---

## ### Шаг 1: Настройка Рабочего ПК (Генерация SSH-ключа)

Этот шаг выполняется один раз для создания безопасных учетных данных для доступа к серверам.

1.  **Сгенерируйте пару SSH-ключей** на вашем рабочем компьютере. Откройте терминал и выполните:
    ```bash
    ssh-keygen -t rsa -b 4096
    ```
    На все вопросы просто нажимайте `Enter`, чтобы создать ключ по умолчанию без парольной фразы.

2.  **Скопируйте ваш ПУБЛИЧНЫЙ ключ.** Это ключ, который вы будете безопасно размещать на серверах.
    ```bash
    cat ~/.ssh/id_rsa.pub
    ```
    Скопируйте всю выведенную строку (начинается с `ssh-rsa...`) в буфер обмена.

> **ВАЖНО:** Файл `~/.ssh/id_rsa` (без `.pub`) — это ваш **приватный ключ**. Никогда и никому его не передавайте.

---

## ### Шаг 2: Настройка Удаленного Сервера (REMOTE-VM)

На этом этапе мы подготовим наш публичный шлюз.

#### 2.1. Подключение к серверу
Подключитесь к `REMOTE-VM` с вашего рабочего ПК:
```bash
ssh пользователь@IP_АДРЕС_REMOTE_VM```

#### 2.2. Установка Nginx
Nginx будет служить нашим веб-шлюзом.
```bash
sudo apt update && sudo apt install nginx -y
```

#### 2.3. Создание пользователя `tunneluser`
Мы создадим специального пользователя с ограниченными правами только для создания туннелей.
```bash
# Создаем пользователя без доступа к командной оболочке
sudo useradd -m -s /usr/sbin/nologin tunneluser

# Создаем для него директорию и файл для ключей
sudo mkdir -p /home/tunneluser/.ssh
sudo touch /home/tunneluser/.ssh/authorized_keys

# Устанавливаем корректные права доступа (критически важно)
sudo chmod 700 /home/tunneluser/.ssh
sudo chmod 600 /home/tunneluser/.ssh/authorized_keys
sudo chown -R tunneluser:tunneluser /home/tunneluser
```

#### 2.4. Добавление вашего публичного ключа
Разрешим нашему ключу доступ к `tunneluser`.
```bash
sudo nano /home/tunneluser/.ssh/authorized_keys
```
1.  Вставьте в редактор ваш **публичный ключ**, скопированный на шаге 1.
2.  **В самое начало вставленной строки** добавьте следующие опции, которые сильно ограничивают права ключа:
    `restrict,permitopen="localhost:8080" `
3.  Итоговая строка должна выглядеть примерно так:
    ```
    restrict,permitopen="localhost:8080" ssh-rsa AAAA... ваш-комментарий
    ```
4.  Сохраните файл и закройте редактор (`Ctrl+X`, `Y`, `Enter`).

#### 2.5. Развертывание конфигурации Nginx
1.  **На вашем рабочем ПК** (в новом терминале), скопируйте файл конфигурации Nginx на сервер:
    ```bash
    scp config/remote-vm/dev.my-tunnel.com.conf пользователь@IP_АДРЕС_REMOTE_VM:~/
    ```
2.  **Вернитесь в терминал с `REMOTE-VM`**. Переместите конфиг и активируйте его:
    ```bash
    sudo mv ~/dev.my-tunnel.com.conf /etc/nginx/sites-available/
    sudo ln -s /etc/nginx/sites-available/dev.my-tunnel.com.conf /etc/nginx/sites-enabled/
    
    # Проверяем синтаксис и перезагружаем Nginx
    sudo nginx -t && sudo systemctl reload nginx
    ```3.  Теперь можно выйти из сессии с `REMOTE-VM`, набрав `exit`.

---

## ### Шаг 3: Настройка Сервера Приложения (LOCAL-VM)

Подготовим машину, на которой будет работать наше приложение и скрипт туннеля.

1.  **На вашем рабочем ПК**, скопируйте скрипт `start_tunnel.sh` на `LOCAL-VM`:
    ```bash
    scp scripts/start_tunnel.sh пользователь@IP_АДРЕС_LOCAL_VM:~/
    ```
    > **ВНИМАНИЕ:** Убедитесь, что вы заменили `IP_АДРЕС_ВАШЕЙ_REMOTE_VM` внутри файла `start_tunnel.sh` перед его копированием!

2.  **Подключитесь к `LOCAL-VM`:**
    ```bash
    ssh пользователь@IP_АДРЕС_LOCAL_VM
    ```

3.  **Сделайте скрипт исполняемым и установите `autossh`:**
    ```bash
    chmod +x ~/start_tunnel.sh
    sudo apt update && sudo apt install autossh -y
    ```
4.  Теперь можно выйти из сессии с `LOCAL-VM`.

---

## ### Шаг 4: Запуск и Тестирование

Все компоненты настроены. Давайте запустим туннель и проверим его работу.

#### **[Терминал 1: Запуск приложения на `LOCAL-VM`]**
1.  Подключитесь к `LOCAL-VM`.
2.  Запустите тестовый веб-сервер на порту `3000`:
    ```bash
    echo "<h1>Туннель успешно развернут!</h1>" > index.html
    python3 -m http.server 3000
    ```
3.  **Оставьте этот терминал работать.**

#### **[Терминал 2: Запуск туннеля на `LOCAL-VM`]**
1.  Откройте **новый терминал** и снова подключитесь к `LOCAL-VM`.
2.  Запустите наш скрипт:
    ```bash
    ./start_tunnel.sh
    ```
3.  Скрипт установит соединение и будет ожидать. **Оставьте этот терминал работать.**

#### **[Терминал 3: Проверка на `REMOTE-VM`]**
1.  Откройте **третий терминал** и подключитесь к `REMOTE-VM`.
2.  Сделайте `curl`-запрос, чтобы проверить, проходит ли трафик:
    ```bash
    curl -H "Host: dev.my-tunnel.com" http://localhost
    ```

#### Ожидаемый результат
В третьем терминале вы должны увидеть HTML-ответ от вашего тестового приложения:
```html
<h1>Туннель успешно развернут!</h1>
```

**Если вы видите это сообщение — развертывание прошло успешно!**